/// Loading and scoring receipt fixtures for the parser's tests and tools.
///
/// Uses `dart:io`, so it lives apart from `receipt_parser.dart`, which the
/// app imports.
library;

export 'src/fixtures/fixture.dart';
